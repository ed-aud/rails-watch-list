<div class="page-body-form">
  <h1>Bookmark a film</h1>

  <div class="form">
    <%= simple_form_for [@list, @bookmark] do |f| %>
      <%= f.association :movie %>
      <%= f.input :comment %>
      <%= f.submit "Add a movie" %>
    <% end %>
  </div>
</div>
