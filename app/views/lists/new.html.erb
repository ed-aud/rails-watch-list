<div class="page-body-form">
  <h1>Create a new list</h1>

  <div class="form">
    <%= simple_form_for(@list) do |f| %>
      <%= f.input :name %>
      <%= f.input :image, as: :file %>
      <%= f.submit %>
    <% end %>
  </div>
</div>
